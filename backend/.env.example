# Optional: OpenAI API Key
# If set, the chatbot will use OpenAI's GPT-3.5-turbo for responses.
# If not set, the chatbot will use safe, rule-based fallback responses.
OPENAI_API_KEY=your_openai_api_key_here

# Flask Environment
FLASK_ENV=development
FLASK_DEBUG=1
